// src/app/(dashboard)/dashboard/layout.tsx
// O layout do dashboard já está no (dashboard)/layout.tsx
// Este arquivo garante que /dashboard funcione corretamente
export default function DashboardPageLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
